// Package acpagent serves Harness as an Agent Client Protocol v1 agent over
// newline-delimited JSON-RPC. It owns protocol lifecycle and translation only;
// construction and execution of a root Harness session remain with the caller.
package acpagent
